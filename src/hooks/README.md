# Hooks

自定义React Hooks目录，用于封装可复用的逻辑。

## Hook规范

- Hook文件名使用camelCase，以`use`开头，如 `useDebounce.ts`
- Hook名与文件名保持一致
- 使用JSDoc注释说明Hook用途和返回值
- 测试文件放在同目录下，命名为 `[hookName].test.ts`

## 示例

参考 `useDebounce.ts` 了解标准Hook结构。

