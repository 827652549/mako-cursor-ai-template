# Components

组件目录用于存放所有可复用的UI组件。

## 组件规范

- 组件文件名使用PascalCase，如 `Button.tsx`
- 组件名与文件名保持一致
- 每个组件都应该有TypeScript类型定义
- 使用JSDoc注释说明组件用途和参数
- 测试文件放在同目录下，命名为 `[ComponentName].test.tsx`

## 示例

参考 `Button.tsx` 了解标准组件结构。

