---
alwaysApply: true
---
# 项目概览

## 项目名称
[Mako Cursor AI Web应用模版]

## 项目描述
[用于快速构建一个MVP应用的模版]

## 技术栈
- 前端：
    - NextJS 16
    - React 19
    - Eslint
    - TypeScript
    - Tailwind CSS v4 + Style-
- 后端：
    - Next.js API Routes（Next.js 内置 API 路由）
    - 当前未集成 Express（未来如需复杂路由可考虑）
- 数据库：
    - 当前未配置数据库
    - 计划使用 PostgreSQL（通过 Drizzle ORM）
    - 按需使用 Redis（缓存）
    - 按需使用 SQLite（开发/测试）
- 测试：
    - Jest + React Testing Library（已配置）
- 部署：
    - Docker（计划中，当前未配置）
- 其他:
    - 依赖安装使用 Bun（推荐）或 npm/yarn
- 代码质量:
    - ESLint 代码检查（必须通过）
    - 运行 `npm run lint` 或 `bun run lint` 检查代码
    - 提交代码前必须确保 lint 检查通过

## 项目结构

每一个目录都应该有对应的README.MD文档, 简要说明目录的职能, 以及告知什么情况下应该编辑此目录的内容

- `src/` - 源代码目录，包含：
    - `app/` - Next.js App Router 路由和页面
    - `components/` - React 组件
    - `hooks/` - 自定义 React Hooks
    - `services/` - 业务逻辑服务
    - `utils/` - 工具函数
    - `constants/` - 常量定义
    - `types/` - TypeScript 类型定义
- `types/` - 根目录类型定义（如路由类型等）
- `public/` - 静态资源目录
- `docs/` - 文档目录
- `tests/` - 测试文件目录
- `.env` - 环境变量
- `.gitignore` - 忽略某些提交, 使用通用模版